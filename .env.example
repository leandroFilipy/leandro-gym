# Banco (local: rode `npx prisma dev` e cole a URL postgres:// que ele mostrar)
DATABASE_URL="postgres://postgres:postgres@localhost:51214/template1?sslmode=disable"
SHADOW_DATABASE_URL="postgres://postgres:postgres@localhost:51215/template1?sslmode=disable"

# Auth.js — gere com: npx auth secret   (ou qualquer string aleatória longa)
AUTH_SECRET="troque-isto-por-uma-string-aleatoria-longa"
AUTH_TRUST_HOST=true

# URL pública do app (usada em links de e-mail)
APP_URL="http://localhost:3000"

# Fase 2 — e-mails e cron (opcionais)
RESEND_API_KEY=""
EMAIL_FROM="Leandro Gym <treino@seudominio.com>"
CRON_SECRET="troque-isto"

# Fase 2 — push notifications (opcional). Gere o par com: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:voce@seudominio.com"

# Fotos de progresso no Vercel Blob PRIVADO (opcional; sem isso ficam no banco).
# Criado com: vercel blob create-store leandro-gym-fotos --access private --region gru1
# `vercel env pull` traz o token para o .env.local.
BLOB_READ_WRITE_TOKEN=""

# Fase 5 — foto do prato com IA (opcional, plano gratuito). Chave em https://aistudio.google.com/apikey
GEMINI_API_KEY=""
# GEMINI_MODEL="gemini-3.8-flash"   # opcional: trocar o modelo sem mexer no código

# E-mails (separados por vírgula) que veem /perfil/erros e recebem push de erro novo
ADMIN_EMAILS=

